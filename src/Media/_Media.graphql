# QUERIES, MUTATIONS, SUBSCRIPTIONS #
type Mutation {
  uploadProfilePhoto(file: Upload!): Photo!
}

# ------------------------ #

type Photo {
  id: String!
  date: DateTime!
  blurHash: String!
  url: String!
  width: Int
  height: Int
}
